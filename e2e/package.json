{
  "name": "trainplex-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "TrainPlex Studio — Playwright E2E smoke tests (Week 8 Step 9.3).",
  "scripts": {
    "test": "playwright test",
    "test:chromium": "playwright test --project=chromium-desktop",
    "test:mobile": "playwright test --project=mobile-safari",
    "test:firefox": "playwright test --project=firefox-desktop",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "typescript": "^5.4.0"
  }
}
