"""TrainPlex peer-review Django app config — Phase 1 Step 6."""

from django.apps import AppConfig


class PeerReviewConfig(AppConfig):
    name = 'peer_review'
    verbose_name = 'TrainPlex Peer Review'
